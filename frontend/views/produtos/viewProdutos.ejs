<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
</head>
<body>

<h1><%= title %></h1>

<% if (typeof message !== 'undefined' && message) { %>
    <p style="color:red;"><%= message %></p>
<% } %>

<form method="POST" action="<%= data && data.produtoid ? '/produtos/update' : '/produtos/insert' %>">

    <% if (data && data.produtoid) { %>
        <input type="hidden" name="produtoid" value="<%= data.produtoid %>">
    <% } %>

    <label>Nome:</label><br>
    <input type="text" name="nome" value="<%= data ? data.nome : '' %>" required><br><br>

    <label>Descrição:</label><br>
    <input type="text" name="descricao" value="<%= data ? data.descricao : '' %>"><br><br>

    <label>Código de Barras:</label><br>
    <input type="text" name="codigobarras" value="<%= data ? data.codigobarras : '' %>" required><br><br>

    <button type="submit">Salvar</button>
    <a href="/produtos">Cancelar</a>

</form>

</body>
</html>